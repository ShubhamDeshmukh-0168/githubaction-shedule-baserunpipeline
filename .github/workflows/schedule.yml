# name: Run Every 5 Minutes

# on:
#   schedule:
#     - cron: '*/5 * * * *'  # Runs every 5 minutes
#   workflow_dispatch:       # Optional: run manually anytime

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Run script
#         run: echo "Pipeline is running every 5 minutes dsd🚀"
