name: Scheduled Pipeline

on:
  schedule:
    - cron: '*/5 * * * *'  # Reliable option
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a script
        run: echo "Runs every 5 minutes"
